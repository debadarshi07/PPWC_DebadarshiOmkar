/*
    Which of the following statements is TRUE for the function prototype declaration given below ?

    (a) P is a function that accepts an argument which is a character array and returns a pointer to an integer quantity.

    (b) P is a function that accepts an argument which is a pointer to a character array and returns a pointer to an integer quantity.

    (c) P is a pointer to a function that accpes an argument which is an array if charater pointers, and returns a pointer to an integer quantity.

    (d) P is a pointer to function that accepts an argument which is a character array and returns a pointer to an integer quantity.

    Answer - (c) P is a pointer to a function that accpes an argument which is an array if charater pointers, and returns a pointer to an integer quantity.
*/
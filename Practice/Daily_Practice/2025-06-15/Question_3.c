/*

Q). Consider binary heap in an array with n elements. It is desired to insert an element into the heap. If a binary search is performed along the path from newly inserted element to the root then the number of comparisons made is _____.

(a). O(logn)            (b). O(n)               (c). O(log(logn))               (d). O(1)

Ans). (c)

*/
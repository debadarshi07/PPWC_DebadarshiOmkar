/*

Q). DFS is performed on a directed acyclic graph. D(u) is discovery time and f(u) is finishing time. Which is true for all the edges (u,v) in the graph ?

(a). d[u] < d[v]        (b). d[u] < f[v]        (c). f[u] < f[v]        (d). f[u] > f[v]

Ans). (d)

*/